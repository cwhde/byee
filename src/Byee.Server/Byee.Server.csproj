<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Byee.Server</RootNamespace>
    <AssemblyName>Byee.Server</AssemblyName>
    <DockerDefaultTargetOS>Linux</DockerDefaultTargetOS>
    <InvariantGlobalization>true</InvariantGlobalization>
    <PublishTrimmed>false</PublishTrimmed>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Embedded script templates with explicit logical names for cross-platform compatibility -->
    <EmbeddedResource Include="Scripts\Installers\install.sh.template" LogicalName="Byee.Server.Scripts.Installers.install.sh.template" />
    <EmbeddedResource Include="Scripts\Installers\install.ps1.template" LogicalName="Byee.Server.Scripts.Installers.install.ps1.template" />
    <EmbeddedResource Include="Scripts\Clients\byee.sh.template" LogicalName="Byee.Server.Scripts.Clients.byee.sh.template" />
    <EmbeddedResource Include="Scripts\Clients\byee.ps1.template" LogicalName="Byee.Server.Scripts.Clients.byee.ps1.template" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="wordlist.txt" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
